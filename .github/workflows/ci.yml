name: CI

on:
  pull_request:
    paths:
      - "**.sql"
      - "briq.yml"
      - "models/**"
      - "tests/**"
  push:
    branches: [main]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.11", "3.12", "3.13"]
      fail-fast: false
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install briq
        run: pip install briq
      - name: Secrets scan
        run: briq scan secrets --fail
      - name: briq build (slim CI — changed models only)
        uses: ./.github/actions/briq-build
        with:
          project-dir: .
          slim-ci: "true"
